{
  "name": "autodeploy",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Running tests now?. Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.6"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
